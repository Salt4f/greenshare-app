<?xml version="1.0" encoding="utf-8" ?>    
<ContentPage
    x:Class="greenshare_app.Views.LoginView"
    xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:viewModel="clr-namespace:greenshare_app.ViewModels"
    >
    <ContentPage.Content>
        <StackLayout BackgroundColor="{StaticResource colorBackground}">
            <Grid Margin="50,25,50,25" VerticalOptions="CenterAndExpand">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <StackLayout Grid.Row="0" Grid.ColumnSpan="2">
                    <Entry
                        x:Name="email"
                        Text="{Binding Email}"
                        Keyboard="Email"
                        Placeholder="Email"
                        Style="{StaticResource styleEntry}"
                        />
                </StackLayout>
                <StackLayout Grid.Row="1" Grid.ColumnSpan="2">
                    <Entry
                        x:Name="password"
                        Text="{Binding Password}"
                        IsPassword="True"
                        Placeholder="Password" 
                        Style="{StaticResource styleEntry}"
                        />
                </StackLayout>
                <StackLayout Grid.Row="3" Grid.Column="0">
                    <FlexLayout>
                        <CheckBox
                        IsChecked="{Binding RememberMe}"
                        />
                        <Label Text="Remember Me" TextColor="{StaticResource colorBlack}" VerticalTextAlignment="Center"/>
                    </FlexLayout>
                </StackLayout>
                <StackLayout Grid.Row="3" Grid.Column="1" Padding="5">
                    <FlexLayout>
                        <Label Text="Forgot Password?" TextColor="{StaticResource colorBlack}" VerticalTextAlignment="Center">
                            <Label.GestureRecognizers>
                                <TapGestureRecognizer Command="{Binding ForgotPasswordCommand}"/>
                            </Label.GestureRecognizers>
                        </Label>
                    </FlexLayout>
                </StackLayout>
                <StackLayout Grid.Row="6" Grid.ColumnSpan="2">
                    <Button x:Name="loginButton" Text="Log in" Style="{StaticResource styleButton}"
                        Command="{Binding LoginButtonCommand}">

                    </Button>
                </StackLayout>
                <StackLayout Grid.Row="7" Grid.ColumnSpan="2">
                    <Button x:Name="registerButton" Text="Sign In" Style="{StaticResource styleButton}"
                        Command="{Binding RegisterButtonCommand}">
                    </Button>
                </StackLayout>
            </Grid>
            <ActivityIndicator IsRunning="{Binding IsBusy}"/>
            
        </StackLayout>
        

    </ContentPage.Content>
</ContentPage>